# État overhaul — start_time (supprimé au commit final Phase 9)

Branche : `refactor/integration-overhaul-2026-07-03`
Tag backup : `backup/before-overhaul-20260703-141815`
HA dev : container `homeassistant` (HA 2026.7.0), config `/home/stephane/homeassistant/config/`

## Phases

- [x] Phase 0 — Préflight (tag + branche)
- [x] Phase 0.5 — État de l'art web → `scratchpad/ha-backend-state-of-art-2026-07-03.md` (résumé §ci-dessous)
- [x] Phase 1 — Découverte + inventaire UID + baseline perf (validée utilisateur)
- [x] Phase 2 — Architecture & lifecycle (déployé, sensor=25.18s vérifié via recorder, 0 erreur)
- [x] Phase 3 — Refactor structurel / code mort (structure appliquée en Phase 2 ; vérifs : ruff F* OK, 0 cycle d'import, pas de code mort)
- [ ] Phase 4 — Client externe : **N/A** (aucune API externe, requirements: [])
- [ ] Phase 5 — Entités (RestoreSensor moderne)
- [ ] Phase 6 — Config flow & translations
- [ ] Phase 7 — Qualité transverse (ruff/mypy/hassfest)
- [ ] Phase 8 — Tests ≥ 90 %
- [ ] Phase 9 — Validation finale + rapport

## Décisions validées par l'utilisateur (2026-07-03)

1. Structure cible : `const.py` + `boot.py` + `diagnostics.py` (pas de api/ ni devices/, disproportionnés).
2. HA minimum : **2025.1.0** (hacs.json, était 2024.4.0).
3. **Suppression du support YAML legacy** (async_setup + async_step_import).
4. Capture boot : **logging.Filter retirable** (async_on_unload) + **RestoreSensor** (persistance).
5. unique_id `"start_time"` **strictement inchangé** — aucune migration.

## Baseline perf (Phase 1.5.bis — copie de /tmp, v1.1.8)

```
Setup of domain start_time took 0.00 seconds
Phase config_entry_setup for start_time took 0.00s (elapsed=0.00s) (wait_time=0.00s)
importtime: custom_components.start_time self=196 µs, cumulé=359 ms (dominé par homeassistant.config_entries, core)
```

## Inventaire unique_id (Phase 1.5)

Une seule entité : `sensor.start_time`, `unique_id = "start_time"` (littéral, `sensor.py:23` avant refactor).

## État de l'art (résumé exploitable, doc complet dans le scratchpad)

- HA stable : 2026.7. `async_forward_entry_setups` (pluriel) obligatoire (singulier supprimé 2025.6).
- `runtime_data` typé via `type XxxConfigEntry = ConfigEntry[XxxData]` (règle Bronze).
- `hass.data["setup_time"]` est devenu privé (`_DATA_SETUP_TIME`) → utiliser le helper public `async_get_setup_timings(hass)` (setup.py:807 en 2026.7).
- Message bootstrap inchangé : `"Home Assistant initialized in %.2fs"` (bootstrap.py:569 en 2026.7).
- Quality Scale du jour : 56 règles (20 Bronze, 10 Silver, 23 Gold, 3 Platinum).
- Tests : pytest-homeassistant-custom-component épinglé sur la cible HA, coverage ≥ 95 % (Silver), 100 % config flow (Bronze).

## Dette critique à corriger

1. mypy casse (`sensor.py:44` — `dict` vs `Any | None`).
2. Monkey-patch `logger.info` jamais retiré + fuite `hass.data[DOMAIN]` à l'unload.
3. API privée `hass.data["setup_time"]` + compat 3 formats → `async_get_setup_timings()`.
4. `Entity`/`_attr_state` legacy → `SensorEntity`/`native_value`, `UnitOfTime.SECONDS`.
5. `DOMAIN` dans sensor.py (import inversé) → const.py.
6. Couverture tests réelle : 0 %.

## Commits des passes

- Phase 2 : 855ef03
