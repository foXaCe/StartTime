#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

pip install -r requirements_dev.txt
command -v prek >/dev/null 2>&1 || pipx install prek
prek install
echo "Setup complete. Next: scripts/lint and scripts/test"
